In Task 2, you will create a webpage that includes a heading, an image, and a short description below it. You will also use CSS to style the page by setting the image width, centering it, and adding spacing above the paragraph. This task helps you practice adding images and basic formatting using HTML and CSS.

















task 9 explanation This code creates a simple webpage that lets the user change the background color. The page includes a dropdown menu where the user can select a color and a button to apply it. When the button is clicked, JavaScript checks the selected color and changes the page’s background to that color. CSS is used to style the layout, center the content, and make the dropdown and button look neat.
